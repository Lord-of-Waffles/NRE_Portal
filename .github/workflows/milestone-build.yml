name: Milestone (Build, Test, Deploy to GCP)

on:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  GCP_REGION: us-central1
  GCP_ARTIFACT_REGISTRY: us-central1-docker.pkg.dev

jobs:
  build:
    name: Build and Push Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set lowercase repository owner
        id: repo
        run: echo "owner=$(echo ${{ github.repository_owner }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT
      
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
      
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
      
      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev
      
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Build and Push API Image
        uses: docker/build-push-action@v5
        with:
          context: WebAPI_NRE-Portal
          file: WebAPI_NRE-Portal/WebAPI_NRE-Portal/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.repo.outputs.owner }}/nre-portal-api:${{ github.run_number }}
            ghcr.io/${{ steps.repo.outputs.owner }}/nre-portal-api:latest
            ${{ env.GCP_ARTIFACT_REGISTRY }}/${{ secrets.GCP_PROJECT_ID }}/nre-portal/nre-portal-api:${{ github.run_number }}
            ${{ env.GCP_ARTIFACT_REGISTRY }}/${{ secrets.GCP_PROJECT_ID }}/nre-portal/nre-portal-api:latest
      
      - name: Build and Push MVC Image
        uses: docker/build-push-action@v5
        with:
          context: WebAPI_NRE-Portal
          file: WebAPI_NRE-Portal/MVC_NRE-Portal/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ steps.repo.outputs.owner }}/nre-portal-mvc:${{ github.run_number }}
            ghcr.io/${{ steps.repo.outputs.owner }}/nre-portal-mvc:latest
            ${{ env.GCP_ARTIFACT_REGISTRY }}/${{ secrets.GCP_PROJECT_ID }}/nre-portal/nre-portal-mvc:${{ github.run_number }}
            ${{ env.GCP_ARTIFACT_REGISTRY }}/${{ secrets.GCP_PROJECT_ID }}/nre-portal/nre-portal-mvc:latest

  test:
    name: Run Unit Tests
    needs: build
    runs-on: windows-latest
    permissions:
      contents: read
      checks: write
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'
      
      - name: Restore packages
        run: dotnet restore WebAPI_NRE-Portal/WebAPI_NRE-Portal.sln
      
      - name: Run xUnit Tests
        run: |
          Get-ChildItem -Recurse -Filter "*_Tests.csproj" | ForEach-Object {
            Write-Host "Running tests for $($_.FullName)"
            dotnet test $_.FullName --configuration Release --logger "trx;LogFileName=test_results.trx"
          }
        shell: pwsh
      
      - name: Publish Test Results
        uses: dorny/test-reporter@v1
        if: always()
        with:
          name: Test Results
          path: '**/test_results.trx'
          reporter: dotnet-trx
          fail-on-error: true

  deploy:
    name: Deploy to Google Cloud Run
    needs: [build, test]
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}
      
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2
      
      - name: Deploy API to Cloud Run
        run: |
          gcloud run deploy nre-portal-api \
            --image ${{ env.GCP_ARTIFACT_REGISTRY }}/${{ secrets.GCP_PROJECT_ID }}/nre-portal/nre-portal-api:latest \
            --platform managed \
            --region ${{ env.GCP_REGION }} \
            --allow-unauthenticated \
            --port 8080 \
            --memory 1Gi \
            --cpu 1 \
            --set-env-vars ASPNETCORE_ENVIRONMENT=Development,ASPNETCORE_URLS=http://+:8080 \
            --project ${{ secrets.GCP_PROJECT_ID }}
          
          API_URL=$(gcloud run services describe nre-portal-api \
            --platform managed \
            --region ${{ env.GCP_REGION }} \
            --format 'value(status.url)' \
            --project ${{ secrets.GCP_PROJECT_ID }})
          
          echo "API_URL=$API_URL" >> $GITHUB_ENV
          echo "API deployed at: $API_URL"
      
      - name: Deploy MVC to Cloud Run
        run: |
          # Use the API_URL from the previous step
          echo "Using API URL: ${{ env.API_URL }}"
          
          gcloud run deploy nre-portal-mvc \
            --image ${{ env.GCP_ARTIFACT_REGISTRY }}/${{ secrets.GCP_PROJECT_ID }}/nre-portal/nre-portal-mvc:latest \
            --platform managed \
            --region ${{ env.GCP_REGION }} \
            --allow-unauthenticated \
            --port 8080 \
            --memory 1Gi \
            --cpu 1 \
            --set-env-vars ASPNETCORE_ENVIRONMENT=Development,ASPNETCORE_URLS=http://+:8080,ApiBaseUrl=${{ env.API_URL }} \
            --project ${{ secrets.GCP_PROJECT_ID }}
          
          MVC_URL=$(gcloud run services describe nre-portal-mvc \
            --platform managed \
            --region ${{ env.GCP_REGION }} \
            --format 'value(status.url)' \
            --project ${{ secrets.GCP_PROJECT_ID }})
          
          echo ""
          echo "========================================="
          echo "Deployment Complete!"
          echo "========================================="
          echo "API: ${{ env.API_URL }}/swagger"
          echo "MVC: $MVC_URL"
          echo "========================================="
